# `jaxfads.nn`

**Source:** `src/jaxfads/nn.py`

Reusable neural-network building blocks:

- `make_mlp`
- readout layers (`StationaryLinear`, `VariantBiasLinear`)
- `DataMasker` for pseudo-observation dropout
